version: '3'
services:
  jwtauth:
    image: "raym123/jwtauth:latest"
    deploy:
      replicas: 2
    hostname: "jwtauth"
    ports:
      - "8085:8085"
    networks:
      - "my-overlay"
    environment:
      - "SERVER_PORT=8085"
      - "SPRING_PROFILES_ACTIVE=dev"

networks:
  my-overlay:
    driver: "overlay"
    external: true